/*

$(window).scroll(function() {
		var scroll = $(window).scrollTop();
    console.log(scroll);
    if (scroll >= 50) {
      // put some documentation here
      $('.js-nav').addClass('scroll-up');
     } else {
      $('.js-nav').removeClass('scroll-up');
    }
});

*/
